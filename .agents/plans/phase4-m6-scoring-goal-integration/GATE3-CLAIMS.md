# Gate-3 Claims (interim form, decision 2026-09-13)

Decision: `.agents/decisions/2026-09-13-interim-goal-and-score-shaping.md`
(default of shared contract D4; operator wording confirmation pending — see
the push ask in the discovery-02 processing handoff).

Trajectory: `discovery-02-main` (log_frames 57064, 34 labeled dumps, emulator
refwork-emu 0.2.3), host capture index at cadence 45 from frame 12 (the first
frame after the game's WRAM clear) plus one mark per operator label (1302
captures). Negatives: `discovery-02-gameover-death` (11319 frames, 262
captures) and `discovery-02-gameover-timer` (35329 frames, 793 captures;
timer expiry is life-lost in this game).

Program: m6-real v3 (blake3 `blake3:b11b07420d75b87ce57c0e1901fa9f2e39cef5a03d5f3c7f6a58a8dbc564f0da`), map v3 (blake3 `blake3:b811fabc28f7daddce8dd44b011bec0e788adc5fa8a76de4e67fd266534081a4`, 24 features,
27 packed bytes), layout (blake3 `blake3:39b8946911334bb7f07d17b6e6cecf955661d7ac9ff4d8bb57a54e4be58f231a`). Private, under `$PR/bundle`.

Labels were generated by `tools/m6-trace-labels.py` from event tables built
from `ramdiff watch` change frames (never from the program); `refwork-verify
trace` compared them against the program-computed values on every capture.

1. Monotone stage component: **PASS** over 1302 captures (non-decreasing
   frame over frame); strict increases at frames 26910 (first_boss), 48105 (world1_boss), 50535 (world1_clear), 54540 (world1_clear)
   (the last is the equipment latch under an unchanged highest stage).
2. Goal fires iff the world-1-clear latch: **PASS**; first true at capture
   frame 50535 (latch frame 50503 from the watch log; no earlier capture is
   true; false on every negative-session capture).
3. Prune: death session — **PASS**: prune true on 63/262 captures, 5/6 death/game-over windows sampled; true from the
   first capture after the lives counter underflows to its game-over sentinel (frame 8454) through
   the game-over screen until continue. Timer session — **PASS**: 17/793 captures, 2/3 death windows sampled
   (life-lost variant; no distinct timer game-over state exists). Main
   session: prune true on 12/1302 captures in 3/4 in-level death windows (two lives lost in W1-S2; no game over); one window shorter than the 45-frame
   cadence holds no capture.
4. Stages not exercised by this trajectory: `capacity_upgrade` (the
   capacity pickup was not repeated; max_health constant).
5. fires-on-credits: **UNDECLARABLE** (interim branch).

Evaluated by: **trace-only** (scorer client pending the state-scorer plan
`interim-goal-score-shaping-validation` WP2 `trajeval`; no index-evaluating
client exists in `state-scorer/crates/scorer-service/src/bin/` as of
2026-09-13). Stage scores in `gate3-eval-*.jsonl` are Σ points over the
trace rows' `active_stages`.

Stability disclosure (WP3 step 7): `level_timer_d100/d10/d1` are marked
stable from single-trace evidence (timer session) plus stage-entry reset
consistency; `score_d100k`, `score_d10`, `score_d1`, `currency_d100`
never changed in any session (adjacency-stitched digits of confirmed arrays).

Commands (run from the repo root; `$PR` = private root, `$ROM` = the single
ROM under `$PR/ROMs/SNES`; `<marks>` = one `--mark <frame>=<label>` per
row of `$PR/discovery-02/labels-<session>.yaml`):

```
refwork-verify host-capture-index --rom $ROM --script $PR/ramdiff/<session>/interactive.padlog \
  --map $PR/bundle/feature-map.yaml --layout $PR/bundle/layout.json --out $PR/host-index/<k> \
  --every 45 --start-frame 12 --frames <log_frames> <marks> \
  --source-ref discovery-02:<session>:emu-0.2.3 --session-name <k> --report $PR/host-index/<k>/report.json
tools/m6-trace-labels.py --events $PR/discovery-02/events-<k>.yaml --index $PR/host-index/<k>/index.jsonl --out $PR/bundle/labels/trace-labels-<k>.yaml
tools/m6-session-pipeline.sh trace --private-root $PR --captures $PR/host-index/main/index.jsonl --labels $PR/bundle/labels/trace-labels-main.yaml
refwork-verify trace --captures $PR/host-index/<k>/index.jsonl --map $PR/bundle/feature-map.yaml --scoring $PR/bundle/scoring-program.yaml \
  --labels $PR/bundle/labels/trace-labels-<k>.yaml --out $PR/bundle/trajectory/negative-<k>.jsonl --report $PR/bundle/validation/trace-report-<k>.json
```

Hashes: host index main `blake3:b635149ddea53fb2a67d895529516cb3cf83a435c65e4fc67ce9ee12324b4b26`, death `blake3:cf04bda23099b6c82072ac4c6a9eb7b9cb7fb16a3245bca0a1474a4cd26e8457`, timer `blake3:19be201b0f123499cdd9186a1432a850678c370a2204a75f2a64d2f8ee1055e9`;
trace out main `blake3:220e05725c9f3305dbf24048e95e1cef911dbca039dea64438c56b9be0d8cb9b`, death `blake3:0964eede6a2e4d0ee85fa812d588616298f0208c0417b7a9cb815c03cd94bb82`, timer `blake3:f52233151df1e9aa35220679e3f2e7cc4870c09a08bcaa792a7e97f857d351e4`;
gate3-eval sha256 (main, death, timer): 5e39c13115df479955f60faf392d4052979a9ef920ac2fb1cf0136270cf90fb7 9dda91c817ebfe143779e415fa8a0b38e9a9022addd26f597d9d4a22ab619a09 162c0f9ff118b1fa0faf0d60e89d479f1c50e8acec44b473f48b70a160ac8bf5 ;
handoff manifest `$PR/bundle/validation/handoff.b3` blake3 `blake3:39aefe5a4602b2c15f463f1f01f367bd463d6837d6eb3d6326cf60be9f88884a`.
