# Operator Ask 1 — Launch Decision Record

Date: 2026-07-12. Operator: Matt Spurlin (run owner unless corrected at
session start). Captured via structured ask in-session.

## Decisions

- **Branch: FULL CORPUS.** Hand-play through first boss + credits/late-game
  goal-positive fixture is schedulable. All downstream packages run;
  Phase 4 exit gate 3 declarable once evidence lands.
- **Session shape: ONE COMBINED SESSION** — 20v offset-discovery segments
  and the gate-4 coherent recorded trajectory in the same sitting.
- **Stack refs CONFIRMED as recorded:** bridge systemd unit → dh-workerd
  `6e348e5`, snapstore durable copy `~/.rbo73/m4-regen-20260707/`, READY
  snapshot `948b73e6`. Agent still verifies stack-up before the session.
- **Private root: EXISTS AND APPROVED.** Location to be provided privately
  at session time — never in this transcript or any public record.

## Checklist items deferred to session-start re-confirmation

Per fast-follow 04 step 1 (session start re-confirms the checklist), the
following are confirmed at the session, recorded only in private evidence:
ROM identity + BLAKE3; credits fixture source (hand-play vs late-game save
RAM); goal-negative coverage; padlog retention / recent-tail policy for
the context fixture; game/revision metadata publication policy.

## Stale-fact note

Fast-follow package 01 step 5 (`refwork-d7t.1` resolution) is satisfied:
that bead closed 2026-07-11 with M2 floor evidence at `9f9b840`.

## Status after this record

- Package 01 (czi): committed at `2827665`; clean-checkout gate running.
- Package 02: **complete** with this record.
- Packages 03/04: unblocked pending the scheduled operator session
  (Ask 2 = the session itself).
