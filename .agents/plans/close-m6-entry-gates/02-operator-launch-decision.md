# Package 02 — Operator Ask 1: Launch Decision (Blocked On Operator)

## Why this is one ask, not several

The fast-follow's package 01 step 4 checklist, the branch selection
(step 6), and the private-root location (step 7) are all operator
decisions with no agent fallback. Batch them into **one** structured
question set. Do not start any private production, intake, or discovery
before the answers are recorded.

## The ask (present verbatim, capture answers durably)

**A. Branch selection** — exactly one:
1. **Full corpus** — a hand-play session through first boss + a
   credits/late-game goal-positive fixture can be scheduled. (Everything
   downstream runs; Phase 4 exit gate 3 declarable.)
2. **First-room fallback** — explicitly approved reduced scope. (Gate 4
   closes for reduced claims; M6 item 3 blocked; exit gate 3 NOT declared;
   follow-on owner recorded for the missing trajectory.)
3. **Blocked** — no approval yet. (Agent finishes fixture-testable work
   and stops; gates 3–4 stay open.)

**B. Checklist** (fast-follow package 01 step 4 — affirmative answers or
"blocked" per item; private details go to private storage, only
yes/no/opaque-ref lands in public records):
- approved single ROM + its BLAKE3 (operator-held);
- run owner;
- approved worker/image/snapshot refs (expected: bridge unit →
  dh-workerd `6e348e5`, snapstore `~/.rbo73/m4-regen-20260707/`,
  READY snapshot `948b73e6` — operator confirms or corrects);
- hand-play trajectory through first boss: schedulable when?
- credits or late-game goal-positive fixture: hand-play, or
  operator-provided save RAM?
- goal-negative examples: ordinary + first-boss states (usually free
  by-products of the session — confirm);
- padlog capture allowed? may a recent input tail be retained in the
  context fixture?
- private storage: root location (outside every checkout), access
  group/token owner, retention, retrieval path, registry-unavailable
  fallback;
- may any game/revision metadata appear in public notes, or must all of
  it stay private?

**C. Session logistics** — can 20v's offset-discovery hand-play and the
gate-4 recorded trajectory share one session (preferred; briefing in
package 03/04), or must they be separate?

## After the answers

1. Record the decision in `.agents/plans/close-m6-entry-gates/GATE-RECORD-ASK1.md`:
   date, branch selected, checklist outcomes (public-safe form), session
   plan. Comment the same on `refwork-20v` and `refwork-5tk`.
2. If branch = full corpus or fallback: operator (or agent under operator
   direction, if access allows) establishes the private root and runs
   `phase4-private-intake` per fast-follow package 01 step 7 — never echo
   ROM filenames or private paths into the transcript.
3. If branch = blocked: stop after packages 01; report which checklist
   items block and end. This plan resumes when the operator re-answers.

## Stale-fact note for the executor

Fast-follow package 01 step 5 requires resolving `refwork-d7t.1` — that
bead **closed 2026-07-11** with its M2 floor evidence; skip that step and
cite the close reason instead of re-litigating it.

## Exit signal

GATE-RECORD-ASK1.md exists naming a branch; beads commented; private root
established (or "blocked" recorded and execution paused).
