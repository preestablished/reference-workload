# Fixture 08: grid room names player_y (volatile). Violates §1.3/7 (room must be stable).
schema_version: 1
kind: feature-map
meta:
  name: demo-game
  workload: refwork-demo
  game_revision: "operator-set"
  version: 1
regions:
  - { name: wram, size: 131072 }
features:
  - name: room_id
    region: wram
    offset: 0x079B
    type: u16le
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: player_y
    region: wram
    offset: 0x0AFA
    type: u16le
    semantics: position_y
    stability: volatile            # volatile — cannot be used as grid room
    discretize: { kind: none }

  - name: player_x
    region: wram
    offset: 0x0AF6
    type: u16le
    semantics: position_x
    stability: volatile
    discretize:
      # room: player_y is volatile — violates §1.3/7
      { kind: grid, x: player_x, y: player_y, room: player_y, cell_w: 32, cell_h: 32 }

  - name: game_mode
    region: wram
    offset: 0x0998
    type: u8
    semantics: mode
    stability: stable
    discretize: { kind: identity }

  - name: credits_flag
    region: wram
    offset: 0x09DA
    type: u8
    semantics: progress_flag
    stability: stable
    discretize: { kind: identity }
