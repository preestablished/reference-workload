# Fixture 15 MAP: valid map with boss_flags (bitflags8, max bit 7).
schema_version: 1
kind: feature-map
meta:
  name: demo-game
  workload: refwork-demo
  game_revision: "operator-set"
  version: 1
regions:
  - { name: wram, size: 131072 }
features:
  - name: boss_flags
    region: wram
    offset: 0x7829
    type: bitflags8              # max bit index is 7
    semantics: progress_flag
    stability: stable
    discretize: { kind: bits }

  - name: game_mode
    region: wram
    offset: 0x0998
    type: u8
    semantics: mode
    stability: stable
    discretize: { kind: identity }

  - name: credits_flag
    region: wram
    offset: 0x09DA
    type: u8
    semantics: progress_flag
    stability: stable
    discretize: { kind: identity }
