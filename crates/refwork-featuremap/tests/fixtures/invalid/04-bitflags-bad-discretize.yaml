# Fixture 04: upgrade_flags (bitflags16le) with discretize: bucket. Violates §1.3/2.
schema_version: 1
kind: feature-map
meta:
  name: demo-game
  workload: refwork-demo
  game_revision: "operator-set"
  version: 1
regions:
  - { name: wram, size: 131072 }
features:
  - name: room_id
    region: wram
    offset: 0x079B
    type: u16le
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: area_id
    region: wram
    offset: 0x079F
    type: u8
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: upgrade_flags
    region: wram
    offset: 0x09A4
    type: bitflags16le
    semantics: flags
    stability: stable
    discretize: { kind: bucket, size: 4 }   # invalid: bitflags requires bits or none

  - name: boss_flags
    region: wram
    offset: 0x7829
    type: bitflags8
    semantics: progress_flag
    stability: stable
    discretize: { kind: bits }

  - name: game_mode
    region: wram
    offset: 0x0998
    type: u8
    semantics: mode
    stability: stable
    discretize: { kind: identity }

  - name: credits_flag
    region: wram
    offset: 0x09DA
    type: u8
    semantics: progress_flag
    stability: stable
    discretize: { kind: identity }
