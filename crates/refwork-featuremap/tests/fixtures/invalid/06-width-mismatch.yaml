# Fixture 06: health (u16le, derived width 2) declared with width: 4. Violates §1.2 width rule.
schema_version: 1
kind: feature-map
meta:
  name: demo-game
  workload: refwork-demo
  game_revision: "operator-set"
  version: 1
regions:
  - { name: wram, size: 131072 }
features:
  - name: room_id
    region: wram
    offset: 0x079B
    type: u16le
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: health
    region: wram
    offset: 0x09C2
    type: u16le
    width: 4            # u16le derives width=2; width:4 is a mismatch
    semantics: health
    stability: stable
    discretize: { kind: threshold, edges: [1, 30, 100, 300] }

  - name: game_mode
    region: wram
    offset: 0x0998
    type: u8
    semantics: mode
    stability: stable
    discretize: { kind: identity }

  - name: credits_flag
    region: wram
    offset: 0x09DA
    type: u8
    semantics: progress_flag
    stability: stable
    discretize: { kind: identity }
