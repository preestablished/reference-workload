# Fixture 02: two features named room_id. Violates §1.3/1 (names must be unique).
schema_version: 1
kind: feature-map
meta:
  name: demo-game
  workload: refwork-demo
  game_revision: "operator-set"
  version: 1
regions:
  - { name: wram, size: 131072 }
features:
  - name: room_id
    region: wram
    offset: 0x079B
    type: u16le
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: room_id
    region: wram
    offset: 0x079D
    type: u8
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: area_id
    region: wram
    offset: 0x079F
    type: u8
    semantics: room_id
    stability: stable
    discretize: { kind: identity }

  - name: game_mode
    region: wram
    offset: 0x0998
    type: u8
    semantics: mode
    stability: stable
    discretize: { kind: identity }

  - name: credits_flag
    region: wram
    offset: 0x09DA
    type: u8
    semantics: progress_flag
    stability: stable
    discretize: { kind: identity }
