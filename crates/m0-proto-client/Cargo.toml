[package]
name = "refwork-m0-proto-client"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
determinism-proto = { workspace = true, features = ["controlplane", "scorer"] }
